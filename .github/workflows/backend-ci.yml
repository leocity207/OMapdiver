name: Backend CI (CMake)

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            g++ \
            libpq-dev

      - name: Configure
        run: cmake -S . -B build -DBUILD_TESTS=ON -DOATPP_BUILD_TESTS=OFF -DOATPP_POSTGRESQL_BUILD_TESTS=OFF

      - name: Build
        run: cmake --build build --parallel

      - name: Run tests
        run: ctest --test-dir build --output-on-failure
FROM python:3.12-slim

WORKDIR /app

ENV PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

RUN pip install psycopg2-binary

# Copy the loader package
COPY db/loader /app/loader

# Copy the JSON data into the image
COPY resources/static/customization/data /app/data

# docker-compose.dev.yml
version: "3.9"

services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    volumes:
      - .:/app
    ports:
      - "8080:8000"
    stdin_open: true
    tty: true